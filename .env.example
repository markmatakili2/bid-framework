# Admin dashboard password (default: mark-admin)
ADMIN_PASSWORD=mark-admin
# Temporary bypass password for admin login
ADMIN_BYPASS_PASSWORD=bypass-admin

# API server port (default: 3001)
PORT=3001

# Optional: enable live AI-generated report insights
OPENAI_API_KEY=

# Optional: override AI model and endpoint
OPENAI_MODEL=gpt-4o-mini
OPENAI_BASE_URL=https://api.openai.com/v1
